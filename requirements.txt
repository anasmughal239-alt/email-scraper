aiohttp>=3.9
beautifulsoup4>=4.12
lxml>=5.2
dnspython>=2.6

# Needed for the Streamlit dashboard (app.py):
streamlit>=1.35
pandas>=2.2

# Needed for --use-playwright / the "Use Playwright fallback" checkbox
# (JS-rendered footer fallback). The Chromium *binary* itself still isn't
# installed by this — see packages.txt and the self-install logic in
# PlaywrightFetcher for that part.
playwright>=1.40
